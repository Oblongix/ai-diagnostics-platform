rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any authenticated user to read and write all documents.
    // WARNING: Broad access for testing only. Revert to stricter rules after verification.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
